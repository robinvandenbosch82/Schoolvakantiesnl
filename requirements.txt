Django==5.1.15          # laatste 5.1.x (security-patches binnen dezelfde minor)
python-dotenv==1.0.1
whitenoise==6.8.2
Pillow==12.2.0          # image pipeline (resize/WebP) + ImageField
requests==2.32.3        # OpenHolidays/Nager/Rijksoverheid/Pexels + foto-downloads
certifi==2025.11.12     # SSL verification voor de externe HTTP-calls
pip-system-certs==5.3   # let requests use the OS trust store, so HTTPS works on
                        # dev machines behind a TLS-scanning antivirus/proxy
                        # (e.g. Avast). Harmless on servers without interception.

# Production (Render / any Django host with managed Postgres)
psycopg2-binary==2.9.9   # PostgreSQL driver
gunicorn==21.2.0         # WSGI server
dj-database-url==2.2.0   # parse DATABASE_URL into Django DATABASES
sentry-sdk==2.20.0       # error tracking (alleen actief met SENTRY_DSN gezet)
